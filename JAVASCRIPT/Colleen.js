// This is the colleen exercise

(function main() {
  //the main code is here
  display(
    `// This is the colleen exercise\n\n(${main.toString()})();\n\n${display.toString()}\n`
  );
})();

function display(arg) {
  console.log(arg);
}
